<div class="container">
      <ul>
        {{#each model.listings as |listing|}}
          {{#link-to 'listing' listing.id}}<li>{{listing.title}} - {{listing.text}}</li>{{/link-to}}
        {{/each}}
      </ul>
</div>
